# Markdown 文档站生成器

一个简单而强大的静态网站生成器，可以将 Markdown 文件自动转换为美观的网页，并部署到 GitHub Pages。

## 特性

- ✨ **简洁美观**：现代化的设计风格，响应式布局
- 🚀 **自动部署**：通过 GitHub Actions 自动部署到 GitHub Pages
- 🎨 **代码高亮**：支持多种编程语言的语法高亮
- 📝 **Frontmatter 支持**：支持在 MD 文件中设置元数据
- 🔍 **SEO 友好**：自动生成页面标题和描述
- 📱 **移动端适配**：完美支持手机和平板设备

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 添加你的 Markdown 文件

将你的 `.md` 文件放入 `markdown` 目录：

```
markdown/
  ├── article-1.md
  ├── article-2.md
  └── subfolder/
      └── article-3.md
```

### 3. 编写 Markdown

你可以在 MD 文件顶部添加 Frontmatter 来设置元数据：

```markdown
---
title: 文章标题
date: 2025-11-03
description: 文章描述
---

# 文章内容

这里是你的文章内容...
```

### 4. 本地预览

```bash
npm run dev
```

这会构建网站并启动本地服务器，浏览器会自动打开预览页面。

### 5. 手动构建

```bash
npm run build
```

构建后的文件会输出到 `dist` 目录。

## 部署到 GitHub Pages

### 方式一：自动部署（推荐）

1. 创建 GitHub 仓库并推送代码

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

2. 在 GitHub 仓库设置中启用 GitHub Pages

   - 进入仓库的 Settings → Pages
   - Source 选择 "GitHub Actions"

3. 推送代码后会自动触发部署

每次推送到 `main` 分支时，GitHub Actions 会自动构建并部署你的网站。

### 方式二：手动部署

1. 本地构建

```bash
npm run build
```

2. 将 `dist` 目录的内容推送到 `gh-pages` 分支

## 自定义

### 修改样式

编辑 `styles.css` 文件来自定义网站样式。主要的 CSS 变量定义在 `:root` 中：

```css
:root {
  --primary-color: #3b82f6;
  --text-color: #1f2937;
  --bg-color: #ffffff;
  /* 更多变量... */
}
```

### 修改网站标题

编辑 `build.js` 中的模板部分：

```javascript
<a href="index.html" class="logo">📝 我的文档站</a>
```

### 自定义首页内容

在 `build.js` 的 `indexContent` 部分修改首页的 hero 区域：

```javascript
<div class="hero">
  <h1>欢迎来到我的文档站</h1>
  <p>基于 Markdown 构建的简洁文档系统</p>
</div>
```

## 项目结构

```
.
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions 配置
├── markdown/               # 存放你的 MD 文件
│   ├── welcome.md
│   └── technical-guide.md
├── dist/                   # 构建输出目录（自动生成）
├── build.js                # 构建脚本
├── styles.css              # 样式文件
├── package.json            # 项目配置
└── README.md               # 说明文档
```

## Frontmatter 字段

支持以下 Frontmatter 字段：

| 字段 | 说明 | 必填 | 默认值 |
|------|------|------|--------|
| title | 文章标题 | 否 | 文件名 |
| date | 发布日期 | 否 | 文件修改时间 |
| description | 文章描述 | 否 | 文章前 150 字符 |

## 支持的 Markdown 语法

- 标题（H1-H6）
- 段落和换行
- **粗体** 和 *斜体*
- 列表（有序和无序）
- 代码块（支持语法高亮）
- 引用
- 链接和图片
- 表格
- 水平分隔线

## 故障排除

### 构建失败

1. 确保已安装所有依赖：`npm install`
2. 检查 Node.js 版本是否 >= 18
3. 检查 markdown 目录是否存在

### GitHub Actions 部署失败

1. 检查仓库的 Pages 设置是否选择了 "GitHub Actions"
2. 确保 `.github/workflows/deploy.yml` 文件存在
3. 查看 Actions 标签页的错误日志

## 许可证

MIT

## 贡献

欢迎提交 Issue 和 Pull Request！
